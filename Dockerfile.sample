FROM &&OS&&:&&DIST&&

ADD php-compiler.sh /root/
RUN chmod +x /root/php-compiler.sh
RUN bash /root/php-compiler.sh

RUN /etc/init.d/php56-fpm start
RUN /etc/init.d/php70-fpm start
RUN /etc/init.d/php71-fpm start
RUN /etc/init.d/php72-fpm start
